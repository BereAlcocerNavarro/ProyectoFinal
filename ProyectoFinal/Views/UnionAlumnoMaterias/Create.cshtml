@* @model ProyectoFinal.Models.UnionAlumnoMateria *@
@model IEnumerable<ProyectoFinal.Models.UnionAlumnoMateria>


@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UnionAlumnoMateria</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

        @Html.DisplayNameFor(model => model.Alumno.Nombre)  
       
        @foreach (var item  in Model)
        {
            @Html.DisplayFor(modelItem => item.Alumno.Nombre)     
        }
 

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
